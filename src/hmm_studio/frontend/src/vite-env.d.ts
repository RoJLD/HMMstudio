/// <reference types="vite/client" />

// Injected at build time by Vite (see vite.config.ts `define`), sourced from
// package.json's `version`. Displayed in the sidebar footer.
declare const __APP_VERSION__: string;
