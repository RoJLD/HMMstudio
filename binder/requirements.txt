# Binder environment for hmm-studio notebook gallery.
#
# This file is consumed by mybinder.org to build a one-click runnable
# environment for the notebooks/ directory. Keep it minimal — only what
# the notebooks themselves import.
#
# The hmm-studio package itself is installed from PyPI (or from the
# Binder build context via binder/postBuild for the latest dev version).

# Core scientific stack
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# HMM engine
hmmlearn>=0.3,<0.4
pyyaml>=6.0

# Jupyter runtime
jupyterlab>=4.0
ipykernel>=6.0
matplotlib>=3.8

# Parquet I/O (some recipes write parquet)
pyarrow>=15.0
